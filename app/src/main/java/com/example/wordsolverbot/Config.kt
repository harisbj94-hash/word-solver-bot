package com.example.wordsolverbot

object Config {

    var wheelRegion = floatArrayOf(0.15f, 0.45f, 0.85f, 0.70f)

    var gridRegion = floatArrayOf(0.05f, 0.12f, 0.95f, 0.40f)

    const val tickIntervalMs = 1600L

    const val gestureDurationMs = 500L

    const val boxBrightnessThreshold = 220
}
